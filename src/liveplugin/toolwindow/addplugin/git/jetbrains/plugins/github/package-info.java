/**
 * This is a partial copy of github plugin code which includes
 * only methods/classes required by {@link liveplugin.toolwindow.addplugin.git.jetbrains.plugins.github.api.GithubApiUtil.getGist()}
 * method.
 *
 * The only reason for copying is to keep liveplugin compatible
 * with IJ12 which doesn't have very good API for fetching gists.
 */
package liveplugin.toolwindow.addplugin.git.jetbrains.plugins.github;