import bar.bar
import liveplugin.show

show(foo())
show(bar())

// Unfortunately, at the moment Kotlin syntax highlighting only works within one file.
// For details and updated please see https://github.com/dkandalov/live-plugin/issues/105.
